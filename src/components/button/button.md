shape: 'pill':
```jsx
['clean', 'white', 'blue'].map(color => (
    <Button
        cssParams={{
            color: color,
            shape: 'pill'
        }}
    >
        Button
    </Button>
))
```
shape: 'rounded':
```jsx
['clean', 'white', 'blue'].map(color => (
    <Button
        cssParams={{
            color: color,
            shape: 'rounded',
        }}
    >
        Button
    </Button>
))
```
