shape: 'pill':
```jsx
['clean', 'white', 'blue'].map(color => (
    <Button
        color={color}
        shape='pill'
    >
        Button
    </Button>
))
```
shape: 'rounded':
```jsx
['clean', 'white', 'blue'].map(color => (
    <Button
        color={color}
        shape='rounded'
    >
        Button
    </Button>
))
```
