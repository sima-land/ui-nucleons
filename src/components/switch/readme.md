Switch представляет собой стилизованный под тумблер `input[type="checkbox"]`.

Стандартный вид переключателя:
```jsx
<div style={{ background: '#fff', padding: 20, fontSize: 24 }}>
  This is just a <Switch /> without props.
</div>
```
